version: 1.0.9

cache: true

endpoints:
  custom:
    - name: "OpenRouter"
      apiKey: "${OPENROUTER_KEY}"
      baseURL: "https://openrouter.ai/api/v1"
      models:
        default:
          - "openai/gpt-4o"
        fetch: true
      titleConvo: true
      titleModel: "current_model"
      summarize: false
      summaryModel: "current_model"
      modelDisplayLabel: "OpenRouter TEST"

    - name: "NanoGPT"
      apiKey: "${NANO_GPT_API_KEY}"
      baseURL: "https://nano-gpt.com/api/v1/"
      models:
        default:
          - "openai/gpt-5.2"
        fetch: true
      addParams:
        reasoning_content_compat: true
      titleConvo: true
      titleModel: "current_model"
      summarize: false
      summaryModel: "current_model"
      modelDisplayLabel: "NanoGPT"

interface:
  memories: true

memory:
  disabled: false
  personalize: true
  tokenLimit: 1200
  messageWindowSize: 4
  validKeys:
    - "user_preferences"
    - "conversation_context"
    - "learned_facts"
  agent:
    provider: "OpenRouter"
    model: "openai/gpt-4o"
